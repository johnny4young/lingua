/**
 * Version of the seeded welcome scratchpad persisted in Settings.
 *
 * Keep this value in a metadata-only module so returning users can decide
 * whether onboarding work is pending without downloading the scratchpad body.
 */
export const SEEDED_SCRATCHPAD_VERSION = 1 as const;
